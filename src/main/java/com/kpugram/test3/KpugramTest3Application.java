package com.kpugram.test3;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Entry point for the KPUgram Test 3 Spring Boot application.
 * This class bootstraps the application using Spring Boot's auto-configuration.
 */
@SpringBootApplication
public class KpugramTest3Application {

	/**
	 * Main method that launches the Spring Boot application.
	 *
	 * @param args Command-line arguments passed during application startup
	 */
	public static void main(String[] args) {
		SpringApplication.run(KpugramTest3Application.class, args);
	}
}
